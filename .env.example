# Copy this to a file named ".env" when you need to override defaults or add secrets.
# Milestone 1 boots fine WITHOUT a .env (compose has a default password).

DB_PASSWORD=changeme

# --- added in later milestones, leave blank for now ---
# JWT_SECRET=replace-with-32-random-bytes
# GROQ_API_KEY=        # free key: https://console.groq.com/keys
# GEMINI_API_KEY=      # free key: https://aistudio.google.com
